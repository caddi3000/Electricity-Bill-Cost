name: Validate
on:
  push:
  pull_request:
jobs:
  basic:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check card exists
        run: test -f utility-bill-card.js
